# Resultado de pruebas - Sprint 2

## Resumen

| Comprobacion | Resultado |
|---|---|
| Suite automatizada | APROBADA |
| Casos ejecutados | 50 |
| Casos aprobados | 50 |
| Casos fallidos | 0 |
| Fixtures de portales | 5 |
| Prueba limitada de Indeed | COMPLETADA CON ERROR DE RED CONTROLADO |

Fecha: 2026-08-11  
Entorno: Windows, Python 3.14.6

## Suite automatizada

Comando:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

Resultado:

```text
.................................................. [100%]
50 passed in 3.16s
```

La suite cubre adicionalmente:

- extracción válida mediante fixtures anonimizadas de Indeed, LinkedIn, OCC, Computrabajo y Glassdoor;
- captcha, login y bloqueo para cada portal;
- JSON-LD malformado y estructura no reconocida;
- validación del contrato único de vacante;
- canonicalización y deduplicación de URLs con rastreo;
- caché vigente, vencida y actualización forzada;
- preservación de errores de búsqueda sin volver a abrirlos como vacantes.

## Prueba controlada con portal vivo

Comando:

```powershell
.\.venv\Scripts\python.exe .\bot_jobs.py --auto-search --profile .\profile.example.json --out .\output --portals indeed --max-results 5 --refresh-cache
```

Resultado:

```text
Listo: output\botjobs_resultados.json
Reporte:
- Detectadas: 5
- Extraidas correctamente: 0
- Preseleccionadas: 0
- Descartadas: 5
- Requieren intervencion: 5
- Cartas generadas: 0
- Cache hits: 0
- Ignoradas en futuro: 0
```

Indeed no fue accesible durante esta ejecución. Las cinco consultas quedaron correctamente registradas con:

```text
estado_extraccion=error_red
requiere_intervencion=si
ignorar_en_futuro=no
accion_recomendada=Reintentar luego o usar --refresh-cache.
```

La primera ejecución de aceptación descubrió que estos errores se reabrían y podían marcarse para ignorar. Se agregó una prueba roja de regresión, se corrigió `extract_links` y la segunda ejecución confirmó el comportamiento seguro.

## Veredicto

Sprint 2 aceptado. La extracción offline es determinista y los fallos del portal vivo se degradan a estados controlados sin perder futuras oportunidades de reintento.
