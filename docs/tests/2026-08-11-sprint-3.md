# Resultado de pruebas - Sprint 3

## Resumen

| Comprobacion | Resultado |
|---|---|
| Suite automatizada | APROBADA |
| Casos ejecutados | 65 |
| Casos aprobados | 65 |
| Casos fallidos | 0 |
| Demo funcional | APROBADA |
| Version del esquema | `1` |
| Decisiones entre ejecuciones iguales | DETERMINISTAS |

Fecha: 2026-08-11  
Entorno: Windows, Python 3.14.6

## Suite automatizada

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

```text
................................................................. [100%]
65 passed in 4.41s
```

Los casos nuevos cubren:

- salario mínimo aceptado y salario inferior descartado;
- máximo de 40 horas aceptado y 41 horas descartadas;
- presencial e híbrido dentro y fuera de CDMX;
- seniority alto, industria bloqueada y trabajo por proyecto;
- trazabilidad mediante flags para cada descarte;
- repetibilidad de score, estado, coincidencias y flags;
- orden estable en empates independientemente del orden de entrada;
- versión explícita del contrato JSON;
- preservación del resultado anterior si falla el reemplazo atómico.

## Demo y esquema

```powershell
.\.venv\Scripts\python.exe .\bot_jobs.py --demo
```

Resultado: una vacante detectada y preseleccionada, una carta generada y código de salida `0`.

```text
schema_version=1
```

## Repetibilidad

Se ejecutó el demo dos veces con las mismas entradas. Antes de comparar se excluyeron solamente:

- `generated_at`;
- el valor de `fecha_generacion` en el resumen.

Resultado:

```text
decisiones_deterministas=True
```

## Escritura segura

Los resultados se escriben primero en un archivo temporal dentro del mismo directorio y después se reemplazan mediante una operación atómica. La prueba inyecta un fallo en el reemplazo y verifica que:

- el JSON anterior permanece intacto;
- el error se propaga;
- el archivo temporal se elimina.

## Veredicto

Sprint 3 aceptado. El ranking es auditable y repetible, el esquema está versionado y una interrupción durante el reemplazo no destruye el último resultado válido.
