# Resultado de pruebas - Sprint 4

## Resumen

| Comprobacion | Resultado |
|---|---|
| Suite automatizada | APROBADA |
| Casos ejecutados | 76 |
| Casos aprobados | 76 |
| Casos fallidos | 0 |
| Flujo CLI CV + decision | APROBADO |

Fecha: 2026-08-11  
Entorno: Windows, Python 3.14.6

## Suite completa

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

```text
........................................................................ [ 94%]
....                                                                     [100%]
76 passed in 8.45s
```

## Comportamientos comprobados

- crear, reemplazar, listar y eliminar decisiones;
- rechazar decisiones fuera de `aprobada`, `descartada` y `revision`;
- asociar una decisión a un CV existente y rechazar uno desconocido;
- validar extensión `.pdf`, firma `%PDF-` y límite de 4 MB;
- generar identificadores estables a partir del contenido;
- evitar duplicar el mismo CV;
- mantener un único CV activo;
- activar otro CV y reasignar el activo tras eliminarlo;
- exigir confirmación literal antes de eliminar;
- rechazar identificadores con secuencias de ruta;
- impedir que el almacén de documentos se configure fuera de `runtime/`.

## Aceptación CLI extremo a extremo

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_state_cli.py -v
```

```text
tests/test_state_cli.py::test_cli_manages_cv_and_decision_end_to_end PASSED
1 passed in 2.17s
```

La prueba crea un PDF aislado, lo agrega mediante `cv add`, obtiene su identificador, aprueba una URL con `decisions set --cv-id` y verifica el registro mediante `decisions list`. El directorio temporal bajo `runtime/` se elimina al terminar.

## Veredicto

Sprint 4 aceptado. Una vacante puede aprobarse y asociarse a un CV utilizando exclusivamente el CLI, con validaciones y confirmaciones de seguridad.
